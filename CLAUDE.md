# CLAUDE.md - Solar Love

## Project Overview

Solar Love is a new project. The repository is currently in its initial setup phase with no source code committed yet. This document should be updated as the project evolves.

## Repository Status

- **State**: Freshly initialized repository
- **Source control**: Git
- **Remote**: Hosted on GitHub-compatible platform

## Getting Started

Once the project is scaffolded, update this section with:
- Prerequisites (Node.js version, package manager, system dependencies)
- Installation steps
- Environment variable setup (`.env` files, secrets)

## Build & Development Commands

_Update this section once the project tooling is configured._

<!-- Example commands to document:
- `npm install` — Install dependencies
- `npm run dev` — Start development server
- `npm run build` — Production build
- `npm run test` — Run all tests
- `npm run test -- path/to/file` — Run a single test file
- `npm run lint` — Lint the codebase
- `npm run format` — Format code
-->

## Project Structure

_Update this section as directories and modules are created._

```
solar-love/
├── CLAUDE.md          # This file - AI assistant guide
└── .git/              # Git infrastructure
```

## Architecture & Key Patterns

_Document the following as the project takes shape:_

- Framework and language choices
- Component/module organization patterns
- State management approach
- Routing strategy
- API layer and data fetching patterns
- Error handling conventions

## Code Conventions

- Write clear, self-documenting code; avoid unnecessary comments
- Keep changes focused and minimal — don't over-engineer
- Follow existing patterns in the codebase when adding new code
- Validate inputs at system boundaries (user input, external APIs), not internally

## Testing

_Update with testing framework, patterns, and commands once configured._

<!-- Example conventions to document:
- Test files live next to source files as `*.test.ts` or in a `__tests__/` directory
- Use descriptive test names that explain the expected behavior
- Run a single test: `npm run test -- path/to/file.test.ts`
-->

## Git Workflow

- Use descriptive commit messages that explain the "why"
- Keep commits focused on a single logical change
- Branch naming: feature branches should be descriptive of the change

## CI/CD

_Update once CI/CD pipelines are configured._

## Environment & Configuration

_Document environment variables and configuration files as they are added._

## Dependencies

_Document key dependencies and their purposes as they are added._
